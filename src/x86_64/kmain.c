#include "eternity.h"
#include "ports.h"

char *cpuVendorId(void);
uint64 logicalCPUnumber(void);
void kmain(void)
{
    // char r[10];
    // itoa(r, cpu,2);
    // serial_putstr(r);
    while (1);
}
